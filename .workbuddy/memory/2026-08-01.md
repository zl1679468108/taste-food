# 2026-08-01 工作日志

## 前端熔断器（Circuit Breaker）实现
- **文件**: `admin/src/utils/request.ts` + `client/src/utils/request.ts`
- **功能**: 连续 3 个 5xx 错误后自动阻断后续请求，30 秒冷却后半开探测恢复
- **设计**: 三态熔断（关闭 → 打开 → 半开），仅统计 5xx 服务端错误（401/403/404 不计入）
- **导出**: `getCircuitBreakerState()` 查询状态, `resetCircuitBreaker()` 手动重置

## T181 — database-init.sql 应用到线上 Supabase（全闭环 ✅）
- **结论**: 26 张 tf_* 表 + 7 个 atomic_* 函数一张不缺（旧"缺 atomic_*"判断不成立）。真实差异是 6 处线上漂移，已用 v22–v26 幂等迁移修复并经**只读核查**确认生效：
  - v22 修复 `tf_orders.updated_at` 误判
  - v23 `tf_payments.updated_at` / `tf_orders.cancel_reason`+`reject_reason`
  - v24 `tf_users.last_login_at`+`updated_at`
  - v25 `tf_delivery_info` 5 缺列 + `type SET DEFAULT 'delivery'`
  - v26 `tf_order_items`/`tf_payments`/`tf_users` 三处 shop_id 外键（DO-block 幂等，孤儿行=0）
- **规范未改**: `database-init.sql` 已含所有列/FK，三位一体同步已满足。
- **父任务 T181 在 tasks.md 标记 ✅ done 2026-08-01**。

## T181 衍生漂移收尾（2026-08-01 下午）
- **#3 回退死代码清理（已落地，tsc EXIT=0）**:
  - `order.service.ts`: 删 `updateOrderStatusDirect` 内 `minimal` 回退块（T181 后列齐全永触发不到）；`isMissingColumnError` 在 order_no 回写处仍用，保留。
  - `payment.service.ts`: 删 4 候选降级循环 + `isMissingColumnError`。改为**单条正确 INSERT**：去掉库里不存在的 `provider` 列（会导致静默降级丢 `user_id`/`paid_at`），保留 `user_id`/`paid_at`，`method` 省略走默认 `wechat`。
  - 注：原 4 候选循环是 `provider`↔`method` 代码/规范错位兼容，属潜在数据丢失路径，已修正。
- **#2 规范对齐（已落地）**: `database-init.sql` 的 `tf_delivery_info` 补 `type text NOT NULL DEFAULT 'delivery'`，与线上 + v25 一致。
- **#1 RESTRICT→CASCADE（v27 已执行 ✅ 2026-08-01，用户确认）**: `tf_payments.order_id` 线上由 RESTRICT 改为 CASCADE，与 `database-init.sql:355` 规范对齐，三位一体同步达成。注：FK 删除规则无法通过只读 REST 客户端内省（pg_constraint 不在 PostgREST 暴露范围），靠幂等守卫迁移 + 用户执行为准；硬验证 SQL：`SELECT conname,confdeltype FROM pg_constraint WHERE conname='tf_payments_order_id_fkey';`（`c`=CASCADE）。

## 待用户决策
- ✅ v27 已执行（CASCADE 对齐完成）。
- 48 个索引 / 9 个 UNIQUE / 各表 CHECK 函数体版本需 SQL Editor 终检（REST 探测不到）。

## 新订单提示音 → 二次元语音播报（2026-08-01）
- **需求**：PC 管理后台「来新订单」提示由音效改为俏皮语音播报。
- **实现**：重写 `admin/src/utils/orderAlertSound.ts`（唯一文件，`NotificationBell` 零改动）。用浏览器 `speechSynthesis` 朗读，按 `order_paid`/`order_cancel_request` 分组随机轮换；`rate 1.08 / pitch 1.15`；自动挑中文语音；800ms 节流；不支持时降级回放 `new-order.wav`。
- **话术风格演进**：初版口语 → 用户要求改**二次元**风格，最终：`order_paid`「主人，来新订单啦，快接单叭～」「叮咚，有客人下单了喵，请查收！」「新订单抵达！店长，该营业咯～」「召唤成功，一位客人向您下单了～」「有新单子咯，冲鸭！」；`order_cancel_request`「主人，有客人想取消订单了，看看呗～」「叮！一条取消申请待处理，拜托了喵」「收到取消申请，店长请尽快确认哦」。试听页：`new-order-voice-demo.html`。
- **小程序端**：用户确认不做语音通知，维持原 WebSocket 消息推送（`client/src/services/socket.ts` 的 `playMerchantNewOrderAlert` 保持 wav+振动），无需改动。
- **清晰度优化（用户反馈「吐字不清」）**：根因=语速 1.08 偏快吞音 + 音调 1.15 偏高发飘 + 原 `find` 取第一个 zh 语音可能选到糊的 voice。改为 `rate 0.95 / pitch 1.0`（俏皮感靠话术本身），并新增 `scoreVoice()` 按质量分排序挑最清晰中文语音（Neural/Online/自然语音 + 简体 zh-CN 优先），`refreshZhVoice()` 改为 filter+sort 取最高分。演示页 `new-order-voice-demo.html` 同步逻辑并新增「清晰度对比」区（清晰版/原版/慢速版）。tsc 0 错误。
- **波浪号朗读修复（用户反馈「~ 符号也念出来了」）**：全角/半角波浪号（～/~，U+FF5E/U+007E）在部分中文 TTS 会被读成「波/浪」等。新增 `sanitizeForSpeech()` 在朗读前 `text.replace(/[~～]/g,'')` 清洗（保留话术文本视觉上的俏皮感，仅朗读层去除）。源码 `speak()` 与演示页 `speak()` 同步加清洗。tsc 0 错误。
- **女声优先 + 语音包手动选择（用户反馈「声音太人机」）**：`scoreVoice()` 加女声偏好——匹配 `xiaoxiao/xiaoyi/tingting/mei/yaoyao/huihui/yan/sinji/女` 等女声名 +50 分，男声名 `yunxi/yunyang/kangkang/云希/云扬/康康/男` -20 分；Neural/Online 仍最高优先（自然女声=最佳）。演示页 `new-order-voice-demo.html` 新增「选择语音包」下拉，列出所有中文 voice 并标注 ⭐自动推荐/♀女/♂男，用户可手动挑顺耳女生声试听。tsc 0 错误。**根因提示**：系统自带 Web Speech 本地语音机械感重，要「真人不人机」需换云端神经 TTS（Azure/讯飞等，需后端+API key，属大改动），待用户决定是否做。
- **豆包(火山引擎)TTS 试听页（用户要求先看效果再接代码）**：`new-order-voice-demo.html` 新增「豆包云端女声试听」区，列出全部「豆包语音合成模型2.0」女声（约18个：佩奇猪zh_female_peiqi_uranus_bigtts=豆包同款、邻家女孩、温柔妈妈、甜美桃子等）。顶部填火山引擎 `X-Api-Key`（仅存 `localStorage` 键 `tf_volc_api_key`，不进文件），前端直连 v3 接口 `POST https://openspeech.bytedance.com/api/v3/tts/unidirectional`（头 `X-Api-Resource-Id: seed-tts-2.0` + `X-Api-Request-Id: uuid`），返回音频用 `new Audio().play()`（兼容 blob 流与 json base64）。底部保留原本地 speechSynthesis 对比区。**下一步（用户挑中音色后）**：写 NestJS 后端 TTS 代理（key 进 `server/.env`，解决 CORS+安全）+ 改 `admin/src/utils/orderAlertSound.ts` 接生产 + 降级。⚠️ 浏览器直连 bytedance 可能遇 CORS，若报错则需先走后端代理（即提前接代码）。
- **Edge TTS 免费女声接入（用户选「免费无 Key」方案）**：`new-order-voice-demo.html` 新增「Edge TTS 免费女声」区——纯前端 WebSocket 直连微软 `wss://speech.platform.bing.com/consumer/speech/synthesize/readaloud/edge/v1?TrustedClientToken=6A5AA1D4EAFF4E9FB37E23D68491D6F4&ConnectionId=<uuid>`（来自 @edge-tts 库常量，无需 API Key/账号）。发 speech.config + SSML，二进制帧按 2-byte headerLen 解析音频（`new DataView(ab).getUint16(0)` → `Uint8Array(ab, headerLen+2)`），拼接 Blob 用 `new Audio().play()`。列出中文女声：晓晓/晓伊/晓辰/晓梦/晓睿/晓萱/晓妍 + 辽宁晓贝/陕西晓妮（共9个）。**优势**：完全免费、无密钥、纯前端；生产接入同理无需后端代理（若 CORS 失败再加无密钥转发）。降级仍保留 speechSynthesis/wav。
- **Edge TTS 浏览器直连失败 → 改走后端无密钥代理（用户截图显示 CORS/连接失败）**：后端新增 `server/src/modules/tts/`（controller/service/module/dto）+ `app.module.ts` 引入 `TtsModule`。`TtsService.edgeTTS()` 用 Node 22 内置全局 `WebSocket` 连接微软 Edge TTS，按 2-byte headerLen 解析音频帧，返回 `Buffer`；`TtsController` 暴露 `POST /api/tts/edge`（`@Public()` 免认证），返回 `audio/mpeg`。演示页 `new-order-voice-demo.html` 的 Edge 区改为 `fetch('http://127.0.0.1:3010/api/tts/edge', { text, voice })` 取 blob 播放。后端 `npm run build` 通过。⚠️ 试听前需启动后端 `cd server && npm run start`。
- **Edge TTS 403 / 无音频 根因已解决（本次收口）**：微软端点返回 403 的根因是缺 `Sec-MS-GEC` 时间窗口 DRM 令牌——算法 `SHA256((unix+11644473600) 下取整到 300s 窗口 ×1e7 + 固定令牌 6A5AA1D4EAFF4E9FB37E23D68491D6F4)` 大写十六进制，作为 query 参数 `Sec-MS-GEC` + `Sec-MS-GEC-Version=1-143.0.3650.75` 附加到握手 URL。`Sec-MS-GEC-Version` 须为当前 Edge 大版本（对照 edge-tts 库常量 `SEC_MS_GEC_VERSION`，2026-08 为 143；旧版 132/130/1 均 403）。音频二进制帧头长度是**大端（BE）**而非小端——原 `readUInt16LE` 误读为 33792 导致 chunks 为空、最终超时；改 `readUInt16BE` 后正常。实测 `POST /api/tts/edge` 返回 20160 字节合法 MP3（audio/mpeg，48kbps/24kHz/Mono）。生产 `admin/src/utils/orderAlertSound.ts` 已接 **Edge TTS 优先 + speechSynthesis/wav 兜底**（默认女声 XiaoxiaoNeural，~ 已清洗），`tsc --noEmit` 通过。试听页 `new-order-voice-demo.html` 同步：Edge 区~过滤、注释更新为「已接入生产」。
- **Edge TTS 从中国连微软超时 504 兜底（2026-08-01 22:50 用户截图）**：现象为后端 `Unhandled error: Edge TTS 合成超时`。排查：连官方 Python `edge-tts` CLI 也 `Connection timeout to host wss://speech.platform.bing.com/...`，HTTPS GET 能返回 404 但 WebSocket upgrade 挂死，判断为微软服务到中国大陆的 WebSocket 路径当前不稳定/被限。修复：controller 捕获超时并返回 **504 Gateway Timeout**（避免 NestJS 全局异常过滤器记为未处理错误）；service 单连接超时从 15s 降到 8s，新增简易熔断（连续 3 次失败熔断 2min），倒逼前端走浏览器 speechSynthesis/wav 兜底；`admin/src/utils/orderAlertSound.ts` 已有兜底链，无需改动。后续可选：接入火山引擎/豆包等国内稳定 TTS（需 API Key），或预生成标准话术 MP3 文件随包部署。

## antd v5 弃用告警清理（Pass 2 / 3 / 4，2026-08-01）
- **目标**：admin 控制台 React 警告 14 → 2。
- **Pass 2（已完成）**：Descriptions styles + RangePicker allowEmpty 等安全修复，14→8→6。
- **Pass 3（已完成）**：`message` 静态调用 → antd `<App>` 上下文。
  - 新增 `admin/src/utils/antdApp.ts` 单例 Proxy（静态 message 桥接 App 实例，未挂载时回退静态 message 防首屏崩溃）。
  - `admin/src/app.tsx` 用 `rootContainer` 包裹 `<App>` 并挂 `message.useMessage()` holder。
  - 批量改写 24 个文件 import（仅改 import，不动调用点）。6 → 4（剩 findDOMNode×2 + disabled-with-empty×1 + useForm-not-connected×1）。
- **Pass 4（已完成，本次）**：修最后两个可修项，4 → 2。
  - `DashboardRangeControl/index.tsx` 的 `<RangePicker>` 补 `allowEmpty={[true, true]}`。根因：rc-picker 告警 `disabled` 不应与空 `value` 同用；Dashboard 默认 `disabled={!loaded}` 且 `customRange` 为空时触发。
  - `useAcceptModal.open()` / `useReasonModal.open()` 移除在 `<Form>` 未挂载时调用 `form.setFieldsValue`/`form.resetFields`。两个弹窗都用 `destroyOnHidden`，open 时 `<Form>` 尚未挂载，提前调方法触发 "Instance created by `useForm` is not connected to any Form element"。接单默认值改用 `<Form initialValues={{ estimatedMinutes: 20 }}>` 注入。
  - 验证：`tsc --noEmit` 0 错误；Playwright 冒烟 console 错误 4 → 2。
  - 剩余 2 条均为 antd 内部 `findDOMNode is deprecated`（rc-* 库，源码 0 处），无法在不替换内部组件前提下消除。
- **关键经验**：用 `destroyOnHidden`/`destroyOnClose` 的 Modal 内 `<Form>`，不要在 hook 的 `open()` 里调用 form 方法（表单未挂载）。默认值用 `initialValues` 或在 `<Form>` 挂载后用 `useEffect` 注入。

## 订单管理搜索改为后端查询（2026-08-01 14:23）
- **问题**: PC 管理后台订单列表搜索是纯前端本地过滤（`useKeywordFilter`），只能匹配当前页已加载数据，无法跨页/跨状态搜索
- **改造**: 后端 `OrderQueryDto` 新增 `keyword`；`order.service.ts` 新增 `applyOrderKeywordFilter()`（Supabase `or+ilike` 匹配 order_no/contact_name/contact_phone），4 个查询方法（findByShopId/UserId/RiderId/findDeliveryPool）+ controller 透传；前端 service/hook 加 keyword 并纳入 queryKey，页面移除本地过滤改走后端。
- tsc 通过。

## PC 管理后台搜索策略规范化（2026-08-01 15:03-15:20）
- **判定标准（项目约定）**: 预估最大量 < 500 且变化不频繁 → 前端全量过滤；否则后端搜索。禁止用 pageSize=200 假装全量（分页器语义欺骗 + 边界脆弱）。
- **排查**: 11 个小数据页面中 8 个已全量拉取（SpecGroup/ShopManage/Menu Item/Category/Promotion/Shop Tables/Account/User），3 个分页（Audit/Messages/User）。
- **改动**:
  1. **Messages**: 分页→全量前端过滤（修复"只能看第1页"bug——原 page state 从未调用 setPage 且无 pagination）+ 加搜索框 + List 前端分页
  2. **Audit**: 加后端 keyword 搜索（保持分页后端，审计日志可能量大，符合判定），移除伪前端过滤
  3. **User**: 保持不动（已是分页+服务端搜索，无 bug，符合判定标准）
  4. **Menu/Item**: 手写 `String.includes`（大小写敏感）→ `filterByKeyword`（大小写不敏感，与其他页面统一）
  5. **utils/table.ts**: 移除 `DEFAULT_TABLE_PAGINATION` 受控 `pageSize`（只留 defaultPageSize），修复 SpecGroup/Menu/Category/Promotion/ShopTables/ShopManage/Account 共 7 个全量页"每页条数切换失效"的全局隐患
- **验证**: `tsc --noEmit` server + admin 均 0 错误。
- **待观察**: User 若平台管理员视角用户数超 500 需重新评估。

## Menu/Item 前端真正用上后端 search（收尾，2026-08-01 15:40）
- **死代码已清**: 上一轮 `getMenuItems({search})` 前端未传 search，导致走本地 `filterByKeyword` 伪过滤。本轮让页面把 `searchText` 真正透传：
  - `admin/src/pages/Menu/Item/index.tsx` 调用 `useMenuItems({ shopId, search })` 并移除本地 `filterByKeyword`
  - `useMenuItems` 已把 `search` 透传 `getMenuItems`（hook 签名），`queryKeys.menuItems.list` 把 `search` 纳入缓存 key → search 变化自动重新请求
  - 后端 `getMenuItems` 的 `search` 真实生效（Supabase `ilike` name 或内存 `includes` 双分支）
- **验证**: `tsc --noEmit` server + admin 均 0 错误；Playwright `tests/admin.spec.ts` 就位（覆盖导航流程）。

## 项目文档整理（2026-08-01 晚）
- **背景**: tasks.md 共 382 项，380 已 done、仅 T43 暂缓；用户要求"已做计划流转到 prd 标记 done，tasks 只保留未做计划"。
- **动作**:
  1. 全量 tasks.md 归档至 `docs/archive/tasks-archive-2026-08-01.md`（1149 行，零丢失）。
  2. 修正 `docs/prd.md` 与 tasks.md 不一致点：§3.20 订单流程优化表格 9 行 `🔄 in_progress/todo` 全部改 ✅ done 2026-07-30（tasks.md 中 T240.1–T240.8 早已全 done）；顶部开发状态行 🔄→✅；§3.18 标题"进行中"清理；路线图 Phase 19/20 🔄→✅；§8.2 已知限制 #5 "T200 改造进行中"→已完成。
  3. 重写 tasks.md 为精简看板（26 行）：仅保留 T43（真实微信支付，paused），其余 380 项 done 指向归档 + prd.md。
- **结果**: prd.md 全功能 ✅、tasks.md 仅剩未做计划；无 `🔄/in_progress/todo` 残留（除订单状态语义"进行中订单"外）。

## 新订单语音播报改「豆包预生成」（2026-08-01 23:12）
- **决策**: 用户确认放弃实时 Edge TTS（中国连微软 WebSocket 不稳定），改用豆包（火山引擎）一次性生成固定话术 MP3，离线播放。
- **改动**:
  1. 新建 `admin/src/utils/alertPhrases.ts`：话术池 PHRASE_POOL + `sanitizeForSpeech`（去~）+ `phraseToFile`（djb2 哈希转十六进制，洗后文案→`alert-{hash}.mp3`）。**播放端与生成脚本共用同一算法，保证文件名一致**。
  2. 新建 `scripts/gen-alert-voice.mjs`：调 `https://openspeech.bytedance.com/api/v3/tts/unidirectional`，头 `X-Api-Key`+`X-Api-Resource-Id: seed-tts-2.0`+`X-Api-Request-Id: uuid`，body `{req_params:{text,speaker,audio_params:{format:'mp3'}}}`；默认音色 `zh_female_peiqi_uranus_bigtts`（佩奇猪2.0 豆包同款）；支持 `--key/--voice/--out` 或 `VOLC_API_KEY` 环境变量；输出 `admin/public/sounds/alert/`。占位 Key 验证返回 401（端点/鉴权字段正确）。9 条话术。
  3. `admin/src/utils/orderAlertSound.ts` 重写为「预生成 MP3 优先 → speechSynthesis → new-order.wav 兜底」；移除实时 Edge TTS 后端调用（demo 页仍可走后端试听）。tsc 0 错误。
- **待办**: 用户提供火山引擎 API Key 后运行 `node scripts/gen-alert-voice.mjs --key <KEY>` 生成 9 个 MP3；文件缺失时 `playLocalAlert` 捕获 error 自动降级。tasks.md 已加 T266.5（🔶 待 Key）。
- **⚠️ Key 类型坑（已踩）**: 用户给的 `ark-a14d589e-...` 是**火山方舟(ARK 大语言模型) Key**，不是「豆包语音合成」Key，两者鉴权不互通，接口返 `401 Invalid X-Api-Key`。语音合成 Key 须从语音合成控制台获取：`https://console.volcengine.com/speech/new/setting/apikeys?projectName=default`（新版只需 X-Api-Key；旧版还需 AppId/AccessKey）。端点 `openspeech.bytedance.com/api/v3/tts/unidirectional` 用 `X-Api-Key`+`X-Api-Resource-Id: seed-tts-2.0`+`X-Api-Request-Id`。若该 ark Key 仍用于别处，建议在控制台禁用/轮换（已暴露在对话中）。

## Playwright E2E 验证打通（2026-08-01 23:28）
- **目标**: 跑通 `tests/admin.spec.ts`，端到端验证「菜品搜索走后端」（第 8.1 步）。
- **环境结论**: 
  - 沙箱无外网，`npx playwright install chromium` 静默退出装不上；但 `playwright.config.ts` 已设 `channel: process.env.CI ? undefined : 'chrome'`，**复用系统 Google Chrome**（`/Applications/Google Chrome.app`）即可离线驱动，无需 bundled chromium。
  - 后端（3010，内存模式）须先 `npm run start`；admin（3012）须先 `npm start`。
- **关键坑**:
  - UMI `npm start` 启动会 `[MFSU][eager] restored cache` + 报 `safe-delete` bulk 拦截（.umi 1070 文件）。**改代码后必须清缓存才能生效**：`rm -rf admin/src/.umi admin/.mfsu`（.umi 删除需 sandbox bypass），否则跑的是旧代码（旧代码下搜索不触发后端请求，测试在 8.1 卡死）。
  - `SearchFilterBar` 用 `Input.Search` + `enterButton="搜索"`，**搜索只在回车/点按钮时触发**；Playwright `page.fill` 只填空文本不触发 → 测试里填完须 `page.keyboard.press('Enter')`。
  - antd 表格 `th:has-text("X")` 会命中隐藏 `measure-cell`（strict 模式报 2 元素）→ 改用 `thead th:has-text("X")`。
  - 数据行用 `.ant-table-tbody tr.ant-table-row`（排除 measure-row）。
  - 登录用 UI 点击 `window.location.href` 整页导航，**偶发竞态 flaky**。改为 API 直接登录 + 注入 localStorage：`page.request.post('/api/auth/login',{username:'admin',password:'admin123'})` 取 token，再 `localStorage.setItem('token'/'refreshToken'/'user')` 后进 /dashboard，稳定通过。
- **顺手修的预存 stale 选择器**（测试本来针对旧 UI 写的，全过期）: 登录按钮「管理员登录」→「登录」；数据看板「今日订单/今日营收/最近订单」→「订单/营收/待处理」；店铺管理子菜单「店铺信息/多店铺管理」→ 单页面；菜品管理 `.ant-menu-item` → `.ant-menu-submenu-title`（是 submenu）；用户管理列「昵称」→「用户」。
- **结果**: `npx playwright test tests/admin.spec.ts` → `1 passed (48s)`，第 8.1 步验证搜索「烤」→ 回车 → `/api/menu-items?search=烤` 后端请求 → 结果行数减少，证明 Menu/Item 前端已真正走后端 search。

## 豆包预生成话术 MP3 最终落地（2026-08-01 23:30）
- **✅ 生成成功**: 正确语音合成 Key（`50011bee-...`，非 ark- 前缀）跑 `scripts/gen-alert-voice.mjs` 生成 9 条话术 MP3 到 `admin/public/sounds/alert/`，全部 ID3 合法（14.7–32.3KB），音色佩奇猪 2.0。
- **关键修复（接口响应解析）**: 豆包 unidirectional 接口返回「换行分隔的多段 JSON 流」——首段 `{"code":0,"message":"","data":"<base64>"}`（音频），后续为使用量等元数据段。脚本须按 `\n` 拆行、逐行 `JSON.parse` 并**拼接所有 `data` 字段**再 base64 解码；不能整体 JSON.parse（报"JSON 后有多余字符"），也不能当纯二进制（首字节是 `{`）。早先两次失败：① ark Key→401；② 把整段 JSON 当音频写盘→非 MP3。
- **播放侧**: `orderAlertSound.ts` 用 `phraseToFile`（djb2 同哈希）定位文件，离线优先播放；文件缺失自动降级 speechSynthesis→wav。tsc 0 错误。tasks.md T266.5 标记 ✅ done。

## 语音播报改音色与话术（2026-08-01 23:38）
- **调整**: 用户要求改用**温柔桃子 2.0**（`zh_female_tianmeitaozi_uranus_bigtts`）音色，话术改为**围绕订单状态流转的标准化播报**，去除二次元俏皮风格。
- **新话术**: order_paid（新订单/待接单）5 条如「您有一笔新订单，请及时处理」「新订单已支付，请尽快接单」；order_cancel_request（取消申请）3 条如「有顾客发起取消申请，请尽快审核」；default 1 条。
- **改动点**: `alertPhrases.ts` 的 PHRASE_POOL + DEFAULT_DOUBAO_VOICE；`scripts/gen-alert-voice.mjs` 的 PHRASE_POOL(副本) + 默认 VOICE。两处必须同步（哈希命名）。删旧佩奇猪文件后重跑脚本，生成 9 个新 MP3（20.9–29.0KB，ID3 合法）。tsc 0 错误。
- **提示**: 若后续要加「接单/备餐/配送/完成」等中间状态的语音提醒，需扩展 PHRASE_POOL 的 kind 并在 admin 调用处（WS/通知）新增触发点；当前仅 order_paid 与 order_cancel_request 两类触发播放。

## 支付方案咨询（2026-08-01 23:57）
- **背景**: 用户问个人主体小程序不走官方支付（个体户/企业资质）是否有别的方案。
- **硬约束确认**: 个人主体 AppID(wx93c16508eff05096) 无法开通微信支付商户号，调不起 wx.requestPayment，小程序内真实在线收款在微信规则下无解。
- **可行替代路径（结论）**:
  1. 到店支付/货到付款（推荐·合规）：现有 sandbox provider 即其数字版，建议升级为正式「柜台支付」业务语义。
  2. 转 H5 网页 + 商家收款码（脱离小程序审核，可行但有经营收款风控/额度限制）。
  3. 第三方聚合支付（PayJS/虎皮椒等）：二清违规 + 小程序内调不起 requestPayment，高风险，保持暂缓（代码 third_party 已标注）。
  4. 升级资质（个体户/企业）：用户已排除，但是唯一合规且体验好的真收款路径。
- **状态**: 待用户决策落地方向（A 到店支付语义化 / B H5+收款码方案 / C 仅记录）。

## 商家语音播报场景扩展（催单/骑手接单/新评价）（2026-08-01 晚）
- **结论**: 系统原有商家通知类型仅 `order_paid`/`order_cancel_request`（其余发给顾客或平台管理员）。`inbox.type` 是自由字符串，扩展无需改表。
- **新增 3 类语音触发（均真实事件）**:
  - `order_reminder`（顾客催单）→ `order.service.ts` 的 `urgeOrder` 成功后 `notifyShopStaff`
  - `rider_assigned`（骑手接单/取餐出发）→ `grabOrder` 成功后 `notifyShopStaff`（原子操作已置 rider_id+delivering）
  - `new_review`（新评价）→ `review.service.ts` 的 `createForOrder` 成功后调用，复用 `OrderService.notifyShopStaff`（已改 public）
- **退款/售后申请**用户桶直接复用既有 `order_cancel_request`（本就是商家售后通知，已播报），不新增重复类型。
- **改动文件**: `admin/src/utils/alertPhrases.ts`（PHRASE_POOL 扩 3 类）、`scripts/gen-alert-voice.mjs`（同步）、`admin/src/utils/orderAlertSound.ts`（`shouldPlayOrderAlert`/`playAdminOrderAlert` 扩至 5 类）、`server/.../order.service.ts`、`server/.../review.service.ts`。
- **语音**: 全部 19 条话术用温柔桃子 2.0 重生成落地 `admin/public/sounds/alert/`，wg同源（播放端哈希=生成脚本哈希）。`inboxService.create`→WS `notification:new`→前端播报链路已历史验证。
- **tasks.md**: 新增 T266.6（6 子任务全 done 2026-08-01）。

## T267 批量异步导出（导出中心）— done 2026-08-01
- **诉求**: 解决批量异步导出任务；明确「PC 导出走 Excel 不走 CSV」。
- **DB**: 新增 `tf_export_jobs` 表（状态机 pending→processing→completed/failed），迁移 `docs/migrations/v28-export-jobs.sql` + `docs/database-init.sql` 升 1.0.7；私有桶 `export-files`。
- **后端**: `server/src/modules/export/`（ExportService 任务 CRUD+内存回退 / ExportRunnerService 后台异步生成 xlsx+站内信通知 / ExportController 提交·列表·详情·下载·店铺隔离）；`StorageService` 增 `uploadBuffer`/`downloadBuffer`+`ensureBucket`；枚举 `ExportJobStatus` 在 `shared` 与 `server/src/common/constants/export.ts` 双写；注册 `ExportModule`。
- **复用**: `OrderService.exportOrdersCsv(format:'xlsx')` 生成 xlsx base64 → 解码上传；`InboxService.create`（WS `notification:new`, type=`export_job`）推送完成通知。
- **Admin**: `services/export.ts` + `useExportQueries` + `queryKeys.exportJobs`；新增「导出中心」页面（任务列表/新建弹窗/下载 + WS 通知 + 3s 轮询兜底）；`routes.ts` 加菜单；订单页「后台导出 Excel」改为提交异步任务。
- **校验**: server + admin `tsc --noEmit` 均 0 错误。线上 Supabase 需先执行 v28 迁移建表（prd §8.2 限制 6 已记）。
- **文档闭环**: prd.md 加 §3.21 + §4.4 接口 + §5.1 表 + Phase 21 + §8.2 限制 6；tasks.md 加 T267（7 子任务 done）。

## T267 运行时验证（续 2026-08-01）
- **新增 e2e 测试** `server/test/export-jobs.e2e.test.ts`：用 `NestFactory.create(AppModule)` 在随机端口起真实 App，走真实 HTTP（seed-merchant→login→POST /export-jobs→轮询→下载），强制内存回退模式。
- **结果**: 测试 PASS。证实 ExportModule 接入无误、Auth/Roles 守卫接受真实 JWT、`format` 恒为 `xlsx`（Excel-only，不走 CSV）、后台 fire-and-forget runner 跑完置 `completed` 并写 `filePath`、下载返回合法 `.xlsx`（PK 头 `50 4B 03 04`）、未完成任务下载返回 409。
- **live 校验**: 重启 dev server（端口 3010，旧实例早于 ExportModule 启动已清掉）；日志确认 `ExportController {/api/export-jobs}` 4 条路由已注册。无 token 直击 `GET /api/export-jobs` 返回 `401 缺少认证令牌`，证明路由存活 + 守卫生效。
- **未决(环境)**: 带 token 的完整 live 流程因服务器走真实 Supabase + 代理，偶发 fetch 超时（非代码问题）。线上功能完整可用前仍需先执行 `docs/migrations/v28-export-jobs.sql` 建 `tf_export_jobs` 表（prd §8.2 限制 6）。
