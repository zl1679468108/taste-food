# 2026-08-05 工作日志

## 站内信方向决策：保留并升级为「双向沟通」（T318）
- 用户确认把站内信从「商家→顾客单向推送」升级为「顾客↔商家双向会话」，任务 T318（含 T318.1–T318.5）已在 `docs/tasks.md` 待办区 + `docs/prd.md` §3.25 规划说明中登记。
- **方向修正**：2026-08-04 曾把「顾客标签」判为伪需求彻底移除，并对「站内信」也存疑（手动运营 ROI 低）；现改为**保留站内信并增强为双向对话**，而非砍掉。
- 关键事实（已核对代码）：
  - `tf_messages` 表结构 `from_user_id / to_user_id / shop_id / content / read_at` 本就是通用的，双向收发**无需改表**，主要靠补接口 + UI。
  - 后端 `MessageController`（`@Controller('merchant/messages')`）目前仅 `@Roles(MERCHANT)` 三个端点（发/发件箱/标记已读），**无顾客侧接口**；小程序 `client/src` 无任何站内信代码。
  - 当前 `PATCH /:id/read`（markRead）是 MERCHANT 手动标记自己发的消息已读，语义有问题；T318.2 改为「顾客打开消息即回写 read_at」。
- 若后续实施：后端新增 `@Roles(CUSTOMER)` 的 `/api/customer/messages`（POST 发/GET 收件箱/PATCH 已读，校验本店下单关系，非本店拒 400）；小程序从零建收件箱/会话页；admin 商家侧会话视图升级为双向。
