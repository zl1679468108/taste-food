# 2026-08-04 工作日志

## 文档收尾（tasks.md 状态一致性）
- 用户选择「仅清理文档」，未执行迁移 apply。
- 修复 tasks.md：
  - 移除「进行中」里重复的 T312.4（已出现在「最近完成」）。
  - 移除「待办」里误留的 T312.7 / T312.11（实际已由 T313.7-9 / T314 实现，状态 done）。
  - 「暂缓/跟进」新增 **M-001**：将待执行迁移 apply 到生产 Supabase（v29/v30 合并文件 + v31 + v32），使用 server/.env.production 的 SUPABASE_SERVICE_ROLE_KEY 执行；SQL 幂等。

## UI 微调
- 用户要求去掉 PC 后台 Dashboard 的「自定义区间」蓝色标签。
- 修改文件：`admin/src/components/DashboardRangeControl/index.tsx`
  - 移除 `{isCustom ? <Tag color="processing">...</Tag> : null}` 块。
  - 保留 RangePicker 与「全店汇总」Tag。
- 已 TypeScript 检查通过；tasks.md「最近完成」追加 T315。

## 待办提醒（未执行，用户下次可选）
- M-001 迁移落地前：顾客管理/标签/站内信跑不了真实数据；v29 缺 `tf_shops.voice_alert_config` 列会导致店铺设置保存报错。
- 当前 PRD 范围内已无未做功能，唯一卡住的是「真实微信支付」（需企业资质）。

## Dashboard「待处理」区改造（不限时间维度）
- 用户诉求：待处理不受时间范围影响，应始终展示所有未处理积压，并让商家点击跳转去处理。决策：拆「待接单(paid) / 待备餐(accepted)」两个可点子项。
- 后端：`order.service.getPendingStats(shopId)` 返回 `{paid,accepted,total}`，统计 `status IN ('paid','accepted')` 且**不限 created_at**；controller 新增 `GET /api/orders/stats/pending`（复用 `resolveAdminShopId`）。
- 前端 service：`admin/src/services/order.ts` 新增 `getPendingStats` + `PendingStats` 类型。
- 前端 hook：`useDashboardQueries.useDashboardStats` 新增 `pendingStats`（逐店 fan-out + 合并，30s 轮询）。
- 组件：新增 `admin/src/components/DashboardPendingCard.tsx`（warning 主色，置于时间范围控件上方常驻区）。
- Dashboard：`index.tsx` 从 `statCards` 移除旧 pending 卡片，渲染新常驻区；`clickable` 仅 `canMerchant` 为 true（平台管理员无订单页，仅展示）。
- 订单页：`Order/index.tsx` 支持从 URL `?status=` 初始化 `activeTab`，并回写 URL。
- 验证：admin + server `tsc --noEmit` 均零错误。

## 订单列表状态标签加数字角标（T316）
- 用户截图要求：小程序订单列表 Tab（全部/待支付/已支付/已接单/制作中/待配送/待取餐/配送中/退款售后/已完成）展示数量角标，同一接口返回 counts，不多次请求，SQL 优化。
- 后端：
  - `server/src/modules/order/order.service.ts`：新增 `OrderStatusCounts` 接口、`countOrdersByScope(scopeType, scopeId, keyword)`、`emptyCounts()`；内存分支用本地聚合兜底。
  - `server/src/modules/order/order.controller.ts`：`GET /api/orders` 返回 `{ ...PaginatedData, counts }`。
  - 数据库：`docs/database-init.sql` 升级到 1.0.8，新增 `count_orders_by_scope` RPC；`docs/migrations/v33-order-status-counts.sql` 幂等可重跑。
- 前端：
  - `shared/src/types/index.ts`：新增 `OrderStatusCounts`。
  - `client/src/components/FilterTabs`：支持 `count` badge（橙色圆点，>99 显示 99+）。
  - `client/src/pages/order-list/index.tsx`：FILTER_TABS 改为 10 个状态粒度；请求 `/orders` 读取 `counts` 并渲染角标。
- 验证：server `npm run build` + `npm test`（95/95 通过）；client `npx tsc --noEmit` 零错误。
- 生产 Supabase 已 apply `docs/migrations/v33-order-status-counts.sql`（`count_orders_by_scope` RPC，2026-08-04 用户提供 PAT 执行）；订单列表数字角标可在真实数据下生效。

## M-001 迁移 apply 完成（2026-08-04 晚）
- 用户提供 Supabase PAT（sbp_...），已用 Management API 落地生产库。**关键修正**：正确端点是 `/v1/projects/{ref}/database/query`（不是 `/sql`，`/sql` 返回 `Cannot POST`）；代理端口动态，需读 `HTTPS_PROXY` 环境变量（本次 58538，旧 50447 已失效）。
- 已 apply 并逐对象验证：
  - v29 `tf_shops.voice_alert_config` 列 ✅
  - v30 `get_today_stats` RPC ✅
  - v31 `tf_users.status` 列(text) ✅
  - v32 `tf_customer_tags` / `tf_customer_tag_relations` / `tf_messages` 三表 ✅
- 执行后已 `rm -f /tmp/.pat` 清理本地 token 文件；tasks.md 把 M-001 从「暂缓/跟进」移入「最近完成」。
- **v33 迁移也已完成**：`docs/migrations/v33-order-status-counts.sql`（T316 订单列表角标所需 `count_orders_by_scope` RPC）由用户于 2026-08-04 提供 PAT 执行 apply，不在 M-001 范围但现也已完成；订单列表数字角标可在真实数据下生效。

## 质量门禁 + 提交（2026-08-04 晚）
- 运行 `scripts/quality-check.js` 全量质量门禁：**9/9 全绿**（shared 类型检查、server 测试+构建、client 类型检查+测试+小程序构建、admin 类型检查+测试+构建），耗时 116.6s。确认迁移 + 跨会话累计改动无回归。
- `git add -A && git commit`：68 files changed，+5819/−397，commit `fc014e9`；记忆文件再补 1 提交 `843e9f8`；两者已 `git push origin master`（`916fa2d..843e9f8`），本地与远端同步。
- 注意：`.workbuddy/memory/` 下的记忆文件也被纳入提交（仅项目事实，无密钥；PAT 已删）。

## 订单状态 count 聚合接口与 SQL 优化（T317）
- 用户截图反馈：PC 后台订单管理页查数据和其它状态 count 调了多个接口，要求集中到一个接口并优化 NestJS/SQL 查询速度。
- 后端：
  - `server/src/modules/order/order.controller.ts` 新增 `GET /api/orders/counts`，复用 `countOrdersByScope` 单条 RPC 返回 `OrderStatusCounts`，不返回订单明细。
  - 权限/查询口径与 `GET /api/orders` 一致（user/rider/shop/pool）。
- 前端：
  - `admin/src/services/order.ts` 新增 `OrderStatusCounts` 类型与 `getOrderStatusCounts`；`getOrders` 返回类型补充 `counts?: OrderStatusCounts`。
  - `admin/src/pages/Order/hooks/useOrderStatusBadges.ts` 改为单次调用 `getOrderStatusCounts`，替代原来 4 次按状态查列表（`pageSize: 1`）的并发请求。
- 数据库：
  - 新增 `docs/migrations/v34-order-counts-perf.sql`：
    - `idx_orders_shop_status`、`idx_orders_status_shop`、`idx_orders_user_status`、`idx_orders_rider_status` 复合索引。
    - `idx_orders_delivery_pool` 部分索引（`delivery_type='delivery' AND rider_id IS NULL`）。
  - `docs/database-init.sql` 索引区同步追加 v34 索引。
- 验证：`server npm run build` + `admin npx tsc --noEmit` 均零错误。
- 待办提醒：v34 迁移尚未 apply 生产 Supabase；如需真实环境生效，需用户提供 PAT 走 Management API `/v1/projects/{ref}/database/query`。

## 运行时联调验收（19:00）—— Dashboard 待处理改造
- 用 Playwright + Chromium 完整跑通商家/平台双视图（`tests/shoot-merchant.mjs` / `shoot-platform2.mjs`）。
- **后端**：admin token `admin/admin123` 登录后 `GET /api/orders/stats/pending?shop_id=...` 返回 `{"paid":1,"accepted":10,"total":11}`；同日 `GET /api/orders/stats/today` 返回 `pendingCount:0`。两接口对照证明了改造口径正确（旧 today 按"今天创建"被掩盖的 11 单积压，新 pending 全量暴露）。
- **前端**：`stats/today`、`stats/daily`、`stats/pending` 三接口在浏览器内 1-3s 全 200；30s 轮询正常。
- **商家视图（`/merchant/dashboard`）**：待处理卡片显示「待接单 1 › / 待备餐 10 ›」（带右箭头可点击）；点「待接单」→ `/merchant/order?status=paid` 且「已支付 1」Tab 激活（1 单）；点「待备餐」→ `/merchant/order?status=accepted` 且「已接单 10」Tab 激活（10 单）。
- **平台管理员视图（`/platform/dashboard`）**：待处理卡片显示「待接单 1 / 待备餐 10」**无右箭头**（clickable=false，因无订单管理页），仅展示数量。
- **关键踩坑（已沉淀 MEMORY.md）**：
  1. Dashboard 路由是 `/platform/dashboard` / `/merchant/dashboard`，**不是 `/dashboard`** — 一开始全部 goto 错路径，UMI 报 `No routes matched`，导致白屏且 stats 请求 0 个（路由出口空）。调试耗了主要时间。
  2. `merchant/merchant123` 商家种子账号在 dev 库 Supabase 真表可登录（首次激活 SEED_PENDING 并写回 password_hash），role=merchant、canMerchant=true、绑 DEFAULT_SHOP_ID。
  3. Playwright 登录标准做法：`page.request.post('/api/auth/login')` 拿 token → `page.evaluate` 注入 localStorage（token/refreshToken/user）→ goto 真实路径；不要走 antd 表单填登录（竞态）。
  4. 验 dashboard 真实数据需 `sleep ≥ 50s`（stats/today + stats/daily + stats/pending 三个 query 全完成才退出 loading；单个 1-3s）。
- 截图存于 `test-results/dashboard-merchant.png`、`order-paid.png`、`order-accepted.png`、`dashboard-platform2.png`。

## 时区口径统一为北京时间（v35，待 apply 生产）
- 用户诉求：统计口径（尤其 Dashboard「今日/趋势」）统一按**北京时间**，不再受 UTC 时区与凌晨失真影响。范围决策：**只改后端统计 RPC 时区口径，前端展示代码不动**。
- 新增迁移 `docs/migrations/v35-timezone-beijing.sql`：`get_today_stats` / `get_daily_stats` 两 RPC 的日期边界与分桶由 `UTC` 改为 `Asia/Shanghai`（聚合逻辑/返回结构不变，幂等可重跑）。
  - `get_today_stats`：`v_today := now() AT TIME ZONE 'Asia/Shanghai'`；`today_orders` 过滤 `created_at >= (v_today::timestamp) AT TIME ZONE 'Asia/Shanghai'`。
  - `get_daily_stats`：默认区间、`(p_start_date::timestamp) AT TIME ZONE 'Asia/Shanghai'` 边界、分桶 `(created_at AT TIME ZONE 'Asia/Shanghai')::date` 均改 Shanghai。
- 源码同步：`docs/database-init.sql` 对应两函数 6 处 `UTC` → `Asia/Shanghai`（含 `v_today` 定义、today 过滤、daily 默认区间/边界/分桶）。
- service 回退一致性：`order.service.getTodayStats` 回退路径原用 `new Date()` 本地 0 点（UTC 环境=UTC 0 点），改为按 `UTC+8` 计算「今日」起点，与 RPC 口径一致（防 RPC 故障时今日口径突变）。`getDailyStats` 回退依赖前端传入的本地日期，属异常分支，本次不动。
- 验证：`server npx tsc --noEmit` 零错误。
- **已 apply 生产**（用户贴 PAT + 要求"记住以后常要执行 SQL"）：
  - PAT 存于 `~/.workbuddy/supabase_pat.txt`（chmod 600，**项目 git 之外**，避免泄露）；apply 脚本与 verify 脚本均改读该文件。
  - `node scripts/apply-v35-timezone.mjs` → 响应 `[]`（CREATE OR REPLACE FUNCTION 成功，exit 0）。
  - `node scripts/verify-timezone.mjs` → `get_today_stats: uses_beijing=true uses_utc=false`、`get_daily_stats: uses_beijing=true uses_utc=false` ✅（两 RPC 均改 Shanghai，无残留 UTC）。
  - 接口回归：`GET /api/orders/stats/today` 与 `/stats/daily?start_date=2026-08-01&end_date=2026-08-04` 仍 `{"code":0,...}` 正常（无回归；今日 0 单与凌晨口径一致，daily 08-01 有 16 单）。
- 产物：`docs/migrations/v35-timezone-beijing.sql`、`docs/database-init.sql`、`server/src/modules/order/order.service.ts`、`scripts/apply-v35-timezone.mjs`、`scripts/verify-timezone.mjs`。
- **已 git commit**：`58b1ab7`「feat(stats): 时区统一北京时间 + 订单 count 性能优化」（16 files, +682/−37），含 T317 v34 代码 + v35 时区 + apply/verify 脚本 + 诊断/验收脚本。本地树干净，**未 push**（用户仅说提交，未说推送）。远端仍为 `843e9f8`。

## v34 索引迁移已 apply 生产（22:22）
- 用户要求"直接走 Management API 执行"。新增 `scripts/apply-v34-order-counts-perf.mjs`（同 PAT 工作流，读 `~/.workbuddy/supabase_pat.txt`）。
- `node scripts/apply-v34-order-counts-perf.mjs` → 响应 `[]`（CREATE INDEX IF NOT EXISTS 成功，exit 0）。
- 新增 `scripts/verify-v34-indexes.mjs`：`node` 执行 → 生产库 `tf_orders` 上 5 个索引全部就位 ✅（`idx_orders_shop_status` / `idx_orders_status_shop` / `idx_orders_user_status` / `idx_orders_rider_status` / `idx_orders_delivery_pool`）。
- 注：verify 脚本第一版用 `params` 占位符被 Management API 拒（返回非数组），已改为 SQL 内联常量名（静态常量无注入风险）解决。
- v34 两个脚本**尚未 git commit**（本次用户只说"执行"，未说提交）；`docs/migrations/v34-order-counts-perf.sql` 已在 `58b1ab7` 入库。
- **22:26 用户再次要求"执行sql"**：重跑 `apply-v34-order-counts-perf.mjs`（响应 `[]`，幂等成功），并用 pg_indexes 直接验证 5 个索引全部就位 ✅；同时确认 `count_orders_by_scope` RPC 仍存在（v33 已 apply），支撑 `GET /api/orders/counts` 的单条聚合查询。结论：v34 生产生效，订单角标 hook 与 counts 接口查询走新索引，无需再发多接口/全表扫描。

## 订单 count 接口合并（v36，约 22:30）
- 用户要求：data + count 集中到 `GET /api/orders` 一个接口（按 status 拆分 counts 在 data.counts 里），删 `GET /api/orders/counts?shop_id=...`。
- 后端：删除 `server/src/modules/order/order.controller.ts` 211-249 行的 `@Get('counts')` `getOrderCounts` 方法；`GET /api/orders` 已内嵌调用 `orderService.countOrdersByScope(...)` 填充 `data.counts`，无需改。
- 前端：
  - `admin/src/services/order.ts` 删 `getOrderStatusCounts`；`getOrders` 返回类型 `counts` 收紧为必填 `OrderStatusCounts`。
  - `admin/src/pages/Order/hooks/useOrderStatusBadges.ts` 重写为纯 hook（`useMemo` 消费 `counts` 入参，移除 WS 订阅与 30s 轮询；轮询由 `useOrders.refetchInterval` 兜底）。
  - `admin/src/pages/Order/index.tsx` 改调入参；新增 useEffect 接管 WS 新单订阅（按 shopId 过滤 + `ordersQuery.refetch()`，带动 counts 一起刷新）。
- 验证：`server npx tsc --noEmit` 0 错；`admin npx tsc --noEmit` 0 错；`admin jest src/__tests__/order.test.ts` 6/6 通过。
- 文档：`docs/tasks.md` T317 备注加 v36 回退；`docs/migrations/v34-order-counts-perf.sql` 与 `docs/database-init.sql` 索引注释里接口名改为「GET /api/orders 内嵌调用」。
- **DB 无新迁移**（索引与 RPC 复用，仅前端两文件加 controller 一段代码改动）。

## 订单 Tab 角标覆盖全部状态（v37，约 22:50）
- 用户反馈：v36 合并接口后，counts 没透到全部 Tab 上（之前 hook 用 BADGE_KEYS 只挑 4 个：`paid`/`ready_for_delivery`/`ready_for_pickup`/`refund`）。
- 决定：counts **全部 10 个状态**应用到 Tab 角标（不做人工挑选）；「全部」tab 显示 `counts.all`；薄包装 hook 一并删除。
- 改动：
  - **删除** `admin/src/pages/Order/hooks/useOrderStatusBadges.ts`（含 `OrderStatusBadges` 类型）。
  - `OrderStatusTabs.tsx` props `badges?: OrderStatusBadges` → `counts?: OrderStatusCounts`；renderLabel 按 `counts[key as keyof OrderStatusCounts]` 取值；首项 `key=''` 走 `counts.all`。
  - `Order/index.tsx` 直接 `counts={ordersQuery.data?.counts}`。
- 零值策略：`renderLabel` 在 `value <= 0` 时不渲染 Badge。截图确认：「待支付 0」「待配送 0」不显示角标。
- 验证：`admin npx tsc --noEmit` 0 错；`admin jest src/__tests__/order.test.ts` 6/6；Playwright 跑 `tests/shoot-order-tabs.mjs` 截图（`test-results/order-tabs-all-badges.png`、`order-tabs-accepted.png`），数字与接口 `data.counts` 完全一致。
- 经验：v36 之后 hook 已退化为字段转发，独立包装价值已失；TS 类型用具体 `OrderStatusCounts` 比 `Record<string, number>` 信息量大；0 值不渲染 Badge 避免噪声。
